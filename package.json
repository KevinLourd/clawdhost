{
  "name": "clawdhost",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @clawdhost/web dev",
    "dev:onboarding": "pnpm --filter @clawdhost/onboarding dev",
    "dev:worker": "pnpm --filter @clawdhost/worker dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  }
}
